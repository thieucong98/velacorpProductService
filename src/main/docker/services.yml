# This configuration is intended for development purpose, it's **your** responsibility to harden it for production
name: velacorpproductservice
services:
  mysql:
    extends:
      file: ./mysql.yml
      service: mysql
  kafka:
    extends:
      file: ./kafka.yml
      service: kafka
  zookeeper:
    extends:
      file: ./kafka.yml
      service: zookeeper
  swagger:
    extends:
      file: ./swagger-editor.yml.yml
      service: swagger
